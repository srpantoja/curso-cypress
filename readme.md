# Pré-condições

> node instalado

# Como instalar as dependências

``` bash

npm install

```

# Como executar o cypress cli

``` bash

npx cypress open

```

# Como executar o cypress headless

``` bash

npx cypress run

```